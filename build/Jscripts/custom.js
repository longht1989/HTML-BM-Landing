/* ====================================
   Onload functions
   ==================================== */

$(function() {
    // aminate fade in index section
    window.sr = ScrollReveal({ reset: true });
    sr.reveal('.foo');
});